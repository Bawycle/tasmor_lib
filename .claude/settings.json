{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cargo check --message-format short 2>&1 | tail -5"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); print('Cargo.lock ne doit pas etre edite manuellement.',file=sys.stderr) or sys.exit(2) if 'Cargo.lock' in p else sys.exit(0)\""
          }
        ]
      }
    ]
  }
}
